#include <stdio.h>
#include <math.h>
short check(int n)
{
  int i;
  for(i=2;i<=sqrt(n);++i)
    {
      
      if(n%i==0)
	return 0;
    }

  return 1;
}

int main()
{
  int i,n,N;
  n=0;N=10000000;
  for(i=n;i<N;++i)
    {
      if(check(i)==1)
	continue;

    }

  return 0;
}
